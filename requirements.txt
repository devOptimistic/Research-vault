# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Server-rendered UI
jinja2>=3.1.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.12

# Validation / settings
pydantic[email]>=2.7.0
pydantic-settings>=2.2.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1  # passlib 1.7.4 is incompatible with bcrypt>=4.1 (AttributeError on hash/verify)

# Task queue
celery[redis]>=5.4.0

# Caching / rate limiting (roadmap feature)
redis>=5.0.0

# HTTP client
httpx>=0.27.0

# Content extraction
readability-lxml>=0.8.1

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
aiosqlite>=0.20.0

# huggingface
huggingface-hub>=1.27.0