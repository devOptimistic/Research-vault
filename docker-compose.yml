version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    # environment:
      # Use socks5h for remote DNS resolution to bypass censorship
      # - HTTP_PROXY=http://host.docker.internal:2080
      # - HTTPS_PROXY=http://host.docker.internal:2080
      # - ALL_PROXY=http://host.docker.internal:2080
      # - NO_PROXY=localhost,127.0.0.1,db,redis,searxng
    extra_hosts:
      - "host.docker.internal:host-gateway"  # Required for Linux
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
      searxng:
        condition: service_started
    volumes:
      - .:/app
    restart: unless-stopped

  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-vault}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-vault}
      POSTGRES_DB: ${POSTGRES_DB:-researchvault}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5430:5432"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-vault} -d ${POSTGRES_DB:-researchvault}",
        ]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6378:6379"
    restart: unless-stopped

  searxng:
    image: searxng/searxng
    # TODO: Remove port mapping for production (searxng container).
    ports:
      - "8080:8080"
    environment:
      - SEARXNG_SECRET=${SEARXNG_SECRET}
      # Use socks5h for remote DNS resolution
      # - HTTP_PROXY=socks5h://host.docker.internal:2080
      # - HTTPS_PROXY=socks5h://host.docker.internal:2080
      # - ALL_PROXY=socks5h://host.docker.internal:2080
      # - NO_PROXY=localhost,127.0.0.1,db,redis,searxng
    extra_hosts:
      - "host.docker.internal:host-gateway"  # Required for Linux
    volumes:
      - ./searxng/settings.yml:/etc/searxng/settings.yml:ro
    restart: unless-stopped

  celery_worker:
    build: .
    command: celery -A app.tasks.celery_app worker --loglevel=info
    env_file:
      - .env
    # environment:
      # The worker needs the proxy to scrape blocked websites
      # - HTTP_PROXY=socks5h://host.docker.internal:2080
      # - HTTPS_PROXY=socks5h://host.docker.internal:2080
      # - ALL_PROXY=socks5h://host.docker.internal:2080
      # - NO_PROXY=localhost,127.0.0.1,db,redis,searxng
    extra_hosts:
      - "host.docker.internal:host-gateway"  # Required for Linux
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    restart: unless-stopped

volumes:
  postgres_data:
  searxng_data: